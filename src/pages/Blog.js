import { Box } from "@mui/material"

const Blog = () => {
    return (
        <Box sx={{ m: "auto", maxWidth:'xl', p:3 }}>
            <h2>This is the Blog page</h2>
        </Box>

    )
}

export default Blog